bazel_dep(name = "rules_rust", version = "0.49.3")  # adjust to latest

# Tell Bazel where to get Rust toolchains
rust = use_extension("@rules_rust//rust:extensions.bzl", "rust")
rust.toolchain(
    edition = "2021"
)

use_repo(rust, "rust_toolchains")

